[package]
name = "reyn-studio"
version = "0.1.2"
edition = "2021"
description = "Native desktop studio for fluid-flow cases, model results, and evidence"

[package.metadata.reyn-macos]
bundle-identifier = "com.reyn.studio"
display-name = "Reyn Studio"
category = "public.app-category.productivity"
minimum-system-version = "11.0"

[dependencies]
eframe = { version = "0.35", default-features = false, features = ["wgpu", "default_fonts", "persistence"] }
egui = "0.35"
bytemuck = { version = "1", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1.0.150"
rfd = "0.17.2"
sha2 = "0.10"
uuid = { version = "1", features = ["v4"] }
flate2 = "1"
fontdue = "0.9"
png = "0.18"
base64 = "0.22"
ed25519-dalek = "3"
getrandom = "0.4"
zeroize = "1"
egui-phosphor = "0.13.0"
ureq = { version = "3.3.0", default-features = false, features = ["json", "native-tls"] }
truck-stepio = "0.3.0"
truck-meshalgo = { version = "0.4.0", default-features = false, features = ["tessellation"] }
zip = { version = "2", default-features = false, features = ["deflate", "deflate-flate2"] }
roxmltree = "0.20"

[target.'cfg(target_os = "macos")'.dependencies]
muda = "0.19.3"
security-framework = { version = "3.7", features = ["OSX_10_15"] }

[target.'cfg(target_os = "windows")'.dependencies]
windows-sys = { version = "0.61.2", features = ["Win32_Foundation", "Win32_Storage_FileSystem", "Win32_System_Com", "Win32_UI_Shell"] }

[build-dependencies]
winres = "0.1.12"

[dev-dependencies]
pollster = "0.4"
# Shader validation without an adapter: the GPU render tests silently skip on
# machines with no wgpu adapter, so WGSL is parsed and validated separately.
naga = { version = "29", features = ["wgsl-in"] }

[profile.dev]
opt-level = 1          # keep the 3D viewport smooth even in debug builds

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
strip = "symbols"
