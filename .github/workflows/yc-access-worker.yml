name: YC access Worker

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  worker:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    defaults:
      run:
        working-directory: services/yc-access-worker
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: services/yc-access-worker/package-lock.json

      - run: npm ci
      - run: npm run check
      - run: npm test
      - run: npm exec wrangler deploy -- --dry-run --outdir "${RUNNER_TEMP}/yc-access-worker"
